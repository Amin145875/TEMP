/ temperature measurements.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
using namespace std;

int main()

{
	//delare varriable and initialisation
	float fahrenheit, celcius;
	char option;
	
	//option for converting celcius to fahrenheit
	if (option == 'a') {
		cout << "input the temperature in celcius:";
		cin >> celcius;
		fahrenheit = (-32.0 * 5.0 / 9.0)
        //temperature conversion formular
			cout << :"n\temperature in degeree fahrenheit:" << fahrenheit << "f"<<end1;
	}
			//option for converting farhrenheiht into celcius
	else if (option == '2') {
		cout << "input temperaure in fahrenheit:";
		cin >> fahrenheit;
		celcius = (fahrenheit - 32) / 5. -//temperature conversion formular
			cout << "\ntemperature in degree celciuse:" << celcius << "c" << end1;
	}
	else
		cout << "erro wrong input." << end1;
	return 0;
	
	cel
		cout<<"fahrenheit-5.0/9.0//temperature"
	}
			"
	}
}
}
}
